# My Calcium PWA - Environment Configuration Template
# Copy this file to .env and update the values for your deployment

# =============================================================================
# CLOUDFLARE WORKER CONFIGURATION
# =============================================================================

# URL of your deployed Cloudflare Worker for sync functionality
# This is provided by the 'wrangler deploy' command output
# 
# SYNC FEATURE TOGGLE:
# - Leave empty or unset to build in STANDALONE MODE (no sync features)
# - Set to your worker URL to build in SYNC MODE (full sync functionality)
#
# Example: https://calcium-sync-worker.your-subdomain.workers.dev
VITE_WORKER_URL=

# OCR.Space API Key
VITE_OCR_API_KEY=

# USDA FDC API Key
VITE_FDC_API_KEY=

# =============================================================================
# DEPLOYMENT PATHS
# =============================================================================

# Production deployment directory (used by deploy.sh)
# Example: /var/www/html or /volume1/web/your-app
PROD_DEPLOY_DIR=

# Development deployment directory (used by deploy.sh)  
# Example: /var/www/html/dev or /volume1/web/your-app-dev
DEV_DEPLOY_DIR=

# =============================================================================
# SCREENSHOT/DEMO MODE
# =============================================================================

# Enable screenshot mode for clean demo URLs (defaults to false)
# Set to 'true' to use example URL instead of real sync URL
VITE_SCREENSHOT_MODE=false

# Example sync URL for screenshots/demos
# Only used when VITE_SCREENSHOT_MODE=true
VITE_EXAMPLE_SYNC_URL=https://mycalcium.app/#sync=demo123&key=example

# =============================================================================
# BUILD MODES
# =============================================================================

# The Calcium Tracker supports two build modes:

# STANDALONE MODE (Default):
# - No sync functionality
# - No Cloudflare setup required
# - Perfect for single-device usage
# - Smaller bundle size
# Setup: Leave VITE_WORKER_URL empty

# SYNC MODE:
# - Full cross-device synchronization
# - Requires Cloudflare Worker setup
# - Enables device pairing via QR codes
# - Cloud backup of all data
# Setup: Configure VITE_WORKER_URL with your Cloudflare Worker URL

# =============================================================================
# NOTES
# =============================================================================

# - VITE_WORKER_URL requires the VITE_ prefix to be accessible in the frontend
# - Deploy paths are only used by the deploy.sh script, not by the application
# - After updating .env, restart your development server for changes to take effect
# - The .env file should never be committed to version control